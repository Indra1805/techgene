"use client"; // Must be at the very top

import CreatePasscodeClient from "./CreatePasscodeClient";

export default function CreatePasscodePage() {
  // This file just wraps the client component
  return <CreatePasscodeClient />;
}
